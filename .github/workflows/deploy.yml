name: Build and Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install & Build
        run: |
          npm install
          npm run build

      - name: Deploy to Hostinger via SFTP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 82.29.191.216
          username: u265693817
          password: ${{ secrets.FTP_PASSWORD }}
          port: 65002
          source: "./dist/*"
          target: "/home/u265693817/domains/infonixsolutions.co.uk/public_html"
          strip_components: 1